# Example environment variables for local development
# Copy this to `.env` and fill real values (do NOT commit `.env`).
FLASK_ENV=development
SECRET_KEY=replace-with-a-secret
DATABASE_URL=sqlite:///./dev.db
HOST=0.0.0.0
PORT=8000

# Optional settings
DEBUG=True
SENTRY_DSN=# Environment Variables (copy to .env and fill in your values)

# Database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/vaayu_drishti

# Logging
LOG_LEVEL=INFO

# Environment
PYTHON_ENV=development

# Optional: Application
WORKERS=4
PORT=8000
HOST=127.0.0.1

# Optional: Monitoring (add your keys)
SENTRY_DSN=
DATADOG_API_KEY=
